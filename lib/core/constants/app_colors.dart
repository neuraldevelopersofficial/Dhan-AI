import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF0B6FFF); // Dhan Blue
  static const Color primaryDark = Color(0xFF0052CC);
  static const Color primaryLight = Color(0xFF4A90E2);

  // Accent
  static const Color success = Color(0xFF16A34A); // Green
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color danger = Color(0xFFEF4444); // Red

  // Neutral - Modern minimal palette
  static const Color surface = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFFAFBFC);
  static const Color mutedText = Color(0xFF6B7280);
  static const Color border = Color(0xFFE5E7EB);
  
  // Modern accent colors
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color dividerColor = Color(0xFFF0F0F0);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF374151);
}

